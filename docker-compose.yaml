services:
  bolglass-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bolglass-web
    restart: always
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - AUTH_URL=${AUTH_URL}
      - AUTH_TRUST_HOST=${AUTH_TRUST_HOST}
      - AUTH_SECRET=${AUTH_SECRET}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
    env_file:
      - .env
    labels:
      - "traefik.enable=true"
      # Phase 1: Test subdomain
      - "traefik.http.routers.bolglass-web-v2.rule=Host(`test.bolann.cloud`)"
      - "traefik.http.routers.bolglass-web-v2.tls=true"
      - "traefik.http.routers.bolglass-web-v2.entrypoints=websecure"
      - "traefik.http.routers.bolglass-web-v2.tls.certresolver=le"
      - "traefik.http.services.bolglass-web-v2.loadbalancer.server.port=3000"
      - "traefik.docker.network=traefik_default"
    volumes:
      - ./data/uploads:/app/apps/web/public/uploads
    networks:
      - traefik-net
      - db-net

networks:
  traefik-net:
    external: true
    name: traefik_default
  db-net:
    external: true
    name: postrges16_internal # Typo from user's screenshot

# Note: No top-level volumes are needed as we use bind mounts.
